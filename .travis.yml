language: objective-c
before_install:
  - brew update
  - brew install xctool || brew upgrade xctool
install: true # skip cocoapods
script: xctool test
notifications:
  email:
    - nolan@nolanw.ca

branches:
  except:
    - iOS8 # until Xcode 6 appears on the test machines
