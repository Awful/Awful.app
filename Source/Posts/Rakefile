require 'open3'

FILES = "zepto.min.js - jquery.mustache.js"

task :default => [ :assemble ]

desc "assemble posts-view.js"
task :assemble do
  Open3.popen3("cat #{FILES} > posts-view.js") do |i, _, _|
    i.puts <<-YO.gsub(/^ */, '')
      
      
      // Alias Zepto as jQuery to keep plugins happy.
      window.jQuery = Zepto
      
      
    YO
    i.close
  end
end
