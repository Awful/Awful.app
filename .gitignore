# Build script working folder
/build.noindex/

# Bundler local install
/.bundle/
/Vendor/bundle/

# Local config for things that shouldn't be checked into a shared repo (see README.md for suggestions).
/Local.entitlements
/Local.xcconfig

# macOS and Xcode junk
.DS_Store
xcuserdata
/Awful.xcworkspace/xcshareddata/Awful.xccheckout

# Optional CSS compiler possible location
/node_modules/

/build-spankykong.sh

*.env
/build/
*.p8
AuthKey_*.p8
/DerivedData/
*.xcarchive
*.ipa
*.dSYM.zip

# Keychain exports (just in case)
*.p12
*.mobileprovision

# Temporary export options
ExportOptions.plist